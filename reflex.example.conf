# restart app server when .go or .gohtml changes
-s -r '\.go|\.gohtml$' -- sh -c 'go run main.go app -c $HOME/etc/dilliver.toml'

# recomile assets when assets/ changes
-r '^assets/' -- sh -c 'npx mix --production'
