<!DOCTYPE html>
<html>
    <head>
        <title>{{block "page_title" .}}{{end}}</title>
    </head>
    <body>
        {{range .Flash}}
        <div>
            {{.Type}}: {{.Body}}
        </div>
        {{end}}

        {{block "body" .}}{{end}}
    </body>
</html>